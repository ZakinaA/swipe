{% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	{{ block("header", "header.html.twig") }}
	<h1>Inscription</h1>
	<div class="form center bg" style="font-weight: bold; text-align: left;">
		{{form_start(form) }}
		{{form_row(form.nom) }}
		{{form_row(form.prenom) }}
		{{form_row(form.dateNaiss) }}
		<button type="submit" style=" background-color: white; color: black;" class="btn btn-primary">Enregistrer</button>
		{{form_end(form) }}</br>
</div>
{{ block("footer", "footer.html.twig") }}{% endblock %}
